import styled from 'styled-components'

const CountSectionContainer = styled.section``

const CountSection: React.FC = () => {
  return <CountSectionContainer>Section</CountSectionContainer>
}
export default CountSection
